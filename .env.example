# Copy this file to `.env` and fill in your keys. DO NOT commit real keys.
OPENAI_API_KEY=
GOOGLE_API_KEY=
# If using a service account for Google ADC, either set this to the path
# or use Streamlit secrets to provide the JSON content under the key
# `GOOGLE_SERVICE_ACCOUNT` and we'll write it to a temp file at runtime.
GOOGLE_APPLICATION_CREDENTIALS=
PREFERRED_PROVIDER=google
